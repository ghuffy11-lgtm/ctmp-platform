# Specs

Build-ready project specifications.

Start with:

```text
implementation-spec.md
```

When major decisions change, update the implementation spec first, then update database/API/frontend artifacts.

