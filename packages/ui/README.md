# Shared UI Package

Shared components used by admin and vendor portals.

Initial components:

```text
DataTable
StatusBadge
WorkflowTimeline
FileUploader
NotificationDrawer
ApprovalCard
PermissionGate
AuditEventList
EnvelopeStatus
SubmissionReceipt
```

Keep shared components generic. Portal-specific workflows should stay inside each app.

